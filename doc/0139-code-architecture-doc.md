# 0139 - 코드 아키텍처 문서 작성

날짜: 2026-09-02
관련: `기획/코드-아키텍처.md` (신규), `Docs/Overview.md`

## 요청 (원문)

> 클로드는 이미지 생성하는 기능이 있나?
> → (없음 — 이미지 읽기·SVG/mermaid·이미지 만드는 코드는 가능, 래스터 아트 생성 불가)
>
> 그럼 지금 이 프로젝트에 작동하는 스크립트들의 코드 아키텍처를 그려서 기획폴더에다가 넣어줄래
> 각 스크립트들이 누구랑 상호작용하고 어떤 데이터, 명령(메소드 호출) 등을 하는지 시각적으로 보이도록
>
> 다른 AI에게 이미지 생성을 요청할수 있도록 상세하게 아키텍처를 설명해도 좋아

## 산출물 — `기획/코드-아키텍처.md`

`Assets/My/Scripts/` 아래 작동 중인 62개 스크립트의 관계도. mermaid 다이어그램 11개:

| § | 다이어그램 |
|---|---|
| 1 | 전체 지도 (6개 서브시스템 그룹) |
| 3 | 데이터 자산(SO) + DialogueImporter 흐름 |
| 4 | 이벤트 버스 (발행 → 구독) |
| 5.1 | 하루 진행 / 환경 / 사운드 |
| 5.2 | 상호작용 코어 (Interactable/Effect/Condition/Interactor) |
| 5.3 | 상호작용 효과 → 시스템 연결 |
| 5.4 | 저녁 접객 (ReceptionManager 손님 큐) |
| 5.5 | 대화 시스템 (DialogueRunner/QuestionPanel/SpeechBubble) |
| 5.6 | 객실 생애주기 (RoomController/GuestManager/KnockEffect/Wallet) |
| 5.7 | 인벤토리 / UI 모드 / HUD |
| 6 | 저녁→새벽→아침 시퀀스 다이어그램 |

+ §2 싱글턴 표, §7 유틸 스크립트 표, §8 한눈 요약,
**§9 "이미지 생성 AI 에게 전달할 상세 설명"** — mermaid 렌더러 없는 도구용 자연어 스펙
(그룹·박스·화살표 3종·라벨·레이아웃 제안 전부 텍스트로).

화살표 표기: 실선=메소드 호출 / 점선=이벤트 구독 / 도트=프로퍼티 읽기.

## 검증

`npx -y @mermaid-js/mermaid-cli@11 -i 기획/코드-아키텍처.md -o out.md` → **11/11 SVG 정상 렌더**.
(초기 오류: `[(...)]` 원통 노드 안 괄호/유니코드 → 텍스트를 `"..."` 로 감싸 해결)

## 참고 — doc 번호 충돌 정리

병행 세션이 `doc/0133`·`0134`(태스크/룸서비스 설계), `doc/0137`(결제 시스템)을 추가함.
이번 세션이 앞서 만든 결정-토픽 문서와 `0137` 이 겹쳐 → **`doc/0138-decision-topics-consumed-after-pick.md`** 로 이동.
코드/문서의 `doc/0137` = 결제, `doc/0138` = 결정 토픽.

## 상태

2026-09-02 완료. 코드 변경 없음 (문서만).
